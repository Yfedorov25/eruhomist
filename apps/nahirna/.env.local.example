# Lead delivery → Telegram (same bot/chat as apps/quadro & apps/eruhomist).
# Set these in Vercel Production env too. Do NOT commit the real .env.local.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Analytics (client-side, public). Empty = analytics no-ops in dev.
NEXT_PUBLIC_GA4_ID=
NEXT_PUBLIC_CLARITY_ID=
